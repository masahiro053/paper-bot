# 開発・改善履歴 (History)

## [2026-06-14] プロンプトの改修と設定ファイルの分離
### 🎯 背景・課題 (Why)
- AIが出力する要約が直訳調で硬く、論文に不慣れなビジネスパーソンがスマホで読むには視認性と実用性が低かった。
- 今後キーワードやプロンプトを頻繁にチューニングすることが予想されるが、すべての処理が `main.py` に集約されていると、誤って処理コードを壊してしまうリスクがあった。

### 💡 改善内容 (What)
1. **プロンプトエンジニアリングの改善**
   - ターゲット読者を「アイデアを探しているビジネスパーソン」と明確に定義。
   - 「背景・アプローチ・結論・実務へのヒント」という4項目の構造化フォーマットを強制し、LINE上での視認性を向上。
2. **コードのモジュール化（リファクタリング）**
   - 検索キーワードやプロンプトなどの設定値のみを `config.py` として分離し、`main.py` から読み込む構造に変更。

### 📊 結果・考察
- LINE上のテキストが箇条書きベースになり、通勤中などのスキマ時間でも直感的に内容が把握できるようになった。保守性も向上し、設定変更時の心理的ハードルが下がった。

---

## [2026-06-15] 状態管理（State Management）の導入による重複排除
### 🎯 背景・課題 (Why)
- ボットの定期運用において、一度配信した論文が数日間にわたって何度も取得・通知されてしまう重複問題が発生する可能性があった。
- GitHub Actionsのような一時的な仮想環境（ステートレス）において、どのように「過去の記憶（送信履歴）」を持たせるかが技術的な課題となった。

### 💡 改善内容 (What)
1. **履歴ファイルによるフィルター実装**
   - 送信済み論文のIDを格納する `history.txt` を作成。次回以降の実行時にこれを読み込み、重複するIDの処理をスキップ（`continue`）するロジックを `main.py` に実装。
   - 重複で弾かれた分を補填するため、APIからの取得バッファを15件に拡張。
2. **CI/CDパイプライン上での自動コミット**
   - GitHub Actionsのステップの最後に、更新された `history.txt` が存在する場合のみ自動で `git commit` と `git push` を行うスクリプトを追加。

### 📊 結果・考察
- 外部のデータベースを用意することなく、リポジトリ単体で完結するミニマムかつ確実な状態管理（重複排除）を実現できた。
- GitHub Actions特有のインフラエラー（Node.jsバージョンの警告や、存在しないファイルのコミットエラー）にも適切に対処し、安定稼働する堅牢なパイプラインが完成した。


---


## [2026-06-21] 検索キーワードのチューニングによる配信クオリティの向上
### 🎯 背景・課題 (Why)
- インフラの自動化と安定稼働（重複排除など）が完了したため、次のフェーズとして「届く論文の質（Signal-to-Noise Ratio）」の向上が求められた。
- 汎用的な機械学習の論文ではなく、広告効果測定や生成AIの実装など、より具体的なビジネス課題の解決に繋がる情報へフォーカスする必要があった。

### 💡 改善内容 (What)
- **検索クエリの高度化**: `config.py` のキーワード群を大幅に見直し。
  - 因果推論の解像度を上げるため `Uplift Modeling` や `Attribution` を追加。
  - 予測モデルの実用性を考慮し `GBDT` や `Ensemble` を追加。
  - LLMのトレンドキャッチアップのため `RAG` や `Agents` を追加。
  - `Marketing Mix Modeling` を対象ドメイン側に移動させ、より高度な数理モデルとの掛け合わせ（例：MMM × Bayesian）がヒットしやすい構造へ変更。

### 📊 結果・考察
- リストの整理と専門用語の拡充により、ボットが「有能なデータマーケターの視点」を持ってarXivをスクリーニングするようになった。
- 運用中のシステムアーキテクチャ（PythonスクリプトやGitHub Actions）には一切手を加えず、設定ファイル（`config.py`）の変数を書き換えるだけでシステムの挙動を大きく進化させることができ、保守性を意識した設計の恩恵を確認できた。