# Architecture Decision Records (ADR)

## ADR 001: 論文取得元としてSemantic ScholarではなくarXiv APIを採用

**Date:** 2026-06-14  
**Status:** Accepted

### Context（背景・課題）
日々のメディアプランニングやデータ分析業務において、最新の技術トレンドをキャッチアップするための自動化システムを構築するにあたり、論文データの取得元を選定する必要があった。当初は「引用数」という定量的な注目度データを持つSemantic Scholar APIの利用も検討した。

### Decision（決定事項）
Semantic Scholarではなく、論文の一次ソースである **arXiv API** を採用し、トレンドの評価は引用数ではなくLLM（Gemini）の文脈理解に委ねる設計とした。

### Rationale（選定理由）
本システムの目的は「毎朝の最新速報」である。Semantic Scholarの引用数データは、論文発表から他の研究に引用されるまでに数ヶ月〜数年のタイムラグが発生するため、発表直後の最新論文（引用数ゼロ）の価値を測れないという致命的なデメリットがあった。
arXivから最新論文を取得し、ビジネスドメインに特化したプロンプトでLLMに審査・要約させるアプローチの方が、タイムラグなしで実務に直結する知見を得られると判断した。

---

## ADR 002: 実行処理と設定値のファイル分割（config.pyの導入）

**Date:** 2026-06-14  
**Status:** Accepted

### Context（背景・課題）
初期開発フェーズでは、APIリクエストからLINEへの送信処理までを `main.py` の1ファイルに集約していた。しかし、検索対象とするキーワード（大カテゴリ・小カテゴリ）やLLMへのシステムプロンプトは、日々の興味関心に合わせて頻繁にチューニングを行う性質のものである。
処理コードと設定値が混在していると、設定変更時に誤ってメインロジックを破壊してしまうリスクが高く、保守性に懸念があった。

### Decision（決定事項）
システムの設定値（キーワードリスト、取得件数、プロンプトテンプレート）を `config.py` に分離し、`main.py` から `import` して読み込むモジュール構造へとリファクタリングを実施した。

### Consequences（結果・影響）
- **メリット:** 処理ロジックの可読性が向上した。また、技術的な知識がなくても `config.py` のテキストを書き換えるだけでボットの振る舞いを安全に変更できるようになり、運用ハードルが大きく下がった。
- **デメリット:** ファイル数が増えたが、現在の小規模な構成においては許容範囲である。

---

## ADR 003: 過去の送信履歴の管理方法としてリポジトリ内のテキストファイル（history.txt）を採用

**Date:** 2026-06-15
**Status:** Accepted

### Context（背景・課題）
システムの運用を開始した結果、毎日arXivから取得する最新論文の中に、前日以前に送信済みの論文が含まれてしまう「重複配信」の課題が浮上した。
本システムはGitHub Actionsを用いたサーバーレス環境で稼働しているため、実行終了後に変数の状態（State）がリセットされてしまう。そのため、送信済みの論文IDをどこかに永続化（保存）して翌日以降に参照する仕組みが必要となった。

### Decision（決定事項）
外部のデータベース（Notion APIやGoogleスプレッドシート等）は使用せず、リポジトリ内に `history.txt` を生成・参照し、GitHub Actionsの実行完了ステップで当該ファイルを自動コミット＆プッシュするアプローチを採用した。

### Rationale（選定理由）
外部データベースを利用するアプローチは、将来的なナレッジベース化（検索・一覧性の向上）の観点では優れている。しかし、現在のフェーズにおいて最も優先すべき要件は「最小の運用コストで確実な重複排除を実現すること」である。
外部APIを利用する場合、新たな認証情報の管理やAPIリクエストの実装コストが発生する。対して本アプローチは、Pythonの標準的なファイルI/OとGitコマンドのみで完結するため、新たな依存関係を増やすことなく堅牢な状態管理を実現できると判断した。

---

## ADR 004: arXiv検索キーワードの再構築とドメイン・手法の掛け合わせ最適化

**Date:** 2026-06-21
**Status:** Accepted

### Context（背景・課題）
初期設定の検索キーワードリストにおいて、「Bayesian」や「Causal Inference」などの重複が存在していた。また、マーケティングや広告領域の最新技術トレンド（生成AIの実務実装や、より高度な効果測定手法）を的確に捕捉するためには、キーワードの粒度と掛け合わせのロジックを見直す必要があった。

### Decision（決定事項）
`config.py` の `MAJOR_KEYWORDS`（対象ドメイン）と `MINOR_KEYWORDS`（具体的手法・技術）を明確に再定義した。
「Marketing Mix Modeling」をドメイン側に昇格させ、手法側には「Uplift Modeling」「RAG」「Agents」など、より具体的で実務解像度の高いキーワード群を新規に追加・整理した。

### Rationale（選定理由）
本ボットの目的は「実務に直結するインスピレーションを得ること」である。
大カテゴリ（業界・課題）と小カテゴリ（解決手段）のAND検索において、手法側のキーワードをより鋭利（シャープ）にすることで、無関係な基礎研究の論文（ノイズ）を弾き、データサイエンスをビジネス実装する上で価値の高い論文（シグナル）の抽出精度を極大化できると判断した。